# ============================================================
#  Tomato Disease Detection -- Python Dependencies
# ============================================================
# For Vercel deployment: uses tflite-runtime (~5MB) instead of
# full TensorFlow (~500MB) to stay within serverless limits.
#
# For local dev with full TF: pip install tensorflow
# ============================================================

tflite-runtime>=2.14.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pillow>=10.2.0
numpy>=1.26.0
